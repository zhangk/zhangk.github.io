---
title: Minicom 串口终端使用指南
slug: 2d601f66747744d8ae4a1fdb169a81c0
tags:
  - minicom
  - linux
date: 2025-06-09T08:32:37.422Z
---

## 🔌 基本连接命令

```bash
sudo minicom -D /dev/ttyUSB0 -b 115200
```

**参数说明**：

| 参数                | 作用                                   |
| ----------------- | ------------------------------------ |
| `sudo`            | 获取root权限（访问串口设备通常需要）                 |
| `-D /dev/ttyUSB0` | 指定串口设备（常见设备：ttyUSB0, ttyS0, ttyAMA0） |
| `-b 115200`       | 设置波特率（常用值：9600, 115200, 1500000）     |

> **注意**：波特率1500000需要内核支持，使用前确认：
>
> ```bash
> sudo stty -F /dev/ttyUSB0 1500000
> ```

## 🛠 完整使用流程

### 1. 安装minicom

```bash
sudo apt install minicom  # Debian/Ubuntu
sudo yum install minicom # CentOS/RHEL
```

### 2. 检测可用串口

```bash
ls /dev/tty*             # 列出所有串口设备
dmesg | grep tty         # 查看最近识别的串口
```

### 3. 连接设备

```bash
sudo minicom -D /dev/ttyUSB0 -b 115200
```

### 4. 退出minicom

| 快捷键            | 功能    |
| -------------- | ----- |
| `Ctrl+A` → `X` | 安全退出  |
| `Ctrl+A` → `Q` | 不重置退出 |
| `Ctrl+A` → `C` | 清除屏幕  |

## ⚙️ 高级配置

### 保存为默认配置

```bash
sudo minicom -s  # 进入配置菜单
```

1. 选择 "Serial port setup"

2. 设置 Device, Baud rate, Hardware Flow Control

3. 选择 "Save setup as dfl"

### 常用快捷键

| 快捷键            | 功能                  |
| -------------- | ------------------- |
| `Ctrl+A` → `Z` | 显示帮助菜单              |
| `Ctrl+A` → `O` | 打开配置菜单              |
| `Ctrl+A` → `W` | 开启/关闭自动换行           |
| `Ctrl+A` → `C` | 清屏                  |
| `Ctrl+A` → `S` | 发送文件（XMODEM/YMODEM） |

## 🧪 波特率1500000特殊配置

### 1. 检查内核支持

```bash
# 查看当前支持波特率
cat /proc/tty/driver/usbserial
```

### 2. 添加自定义波特率（如不支持）

```bash
sudo setserial /dev/ttyUSB0 spd_cust divisor 7692 # 1500000波特率计算值
```

### 3. 连接命令

```bash
sudo minicom -D /dev/ttyUSB0 -b 1500000
```

## 🚨 常见问题处理

### 权限问题

```bash
sudo usermod -aG dialout $USER  # 将用户加入dialout组
newgrp dialout                  # 立即生效
```

### 设备忙/无法访问

```bash
sudo fuser -k /dev/ttyUSB0      # 结束占用进程
sudo chmod 666 /dev/ttyUSB0     # 临时修改权限
```

### 乱码问题

1. 检查波特率是否匹配

2. 检查流控设置（通常关闭 Hardware Flow Control）

3. 尝试不同字符编码：

   ```bash
   minicom -D /dev/ttyUSB0 -b 115200 --encoding utf-8
   ```

## 📁 日志记录功能

```bash
# 启动时自动记录日志
sudo minicom -D /dev/ttyUSB0 -b 115200 -C minicom.log

# 实时切换记录
Ctrl+A → L  # 开始/停止记录
```

## 💡 替代方案推荐

1. **screen**（轻量级）：

   ```bash
   sudo screen /dev/ttyUSB0 115200
   ```

2. **picocom**（更现代）：

   ```bash
   sudo apt install picocom
   sudo picocom -b 115200 /dev/ttyUSB0
   ```

3. **putty**（Windows用户）：

   - 选择Serial连接，设置COM端口和波特率
