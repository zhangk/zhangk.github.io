---
title: Ubuntu ToDesk 无法远程连接问题解决方案
slug: 41d75d8fce9a47c183295e3b7b21d58d
tags:
  - linux
date: 2025-06-10T03:57:26.008Z
---

## 🔧 问题描述

在 Ubuntu 上安装 ToDesk 后，可能出现以下问题：

- 能启动 ToDesk 但无法建立远程连接
- 远程连接后黑屏或无画面
- 提示 "无法连接到显示设备"
- 无法控制远程桌面

## 💡 根本原因

Ubuntu 默认使用 **Wayland 显示服务器**，而 ToDesk 目前不完全兼容 Wayland，需要切换回传统的 **Xorg 显示服务器**。

***

## 🛠️ 解决方案：禁用 Wayland

### 1. 编辑 GDM3 配置文件

```bash
sudo nano /etc/gdm3/custom.conf
# 或使用 vim
sudo vi /etc/gdm3/custom.conf
Debain(/etc/gdm3/daemon.conf)
```

### 2. 修改配置文件

找到以下行并**取消注释**（删除行首的 `#`）：

```ini
#WaylandEnable=false
```

改为：

```ini
WaylandEnable=false
```

**修改后效果**：

```ini
[daemon]
WaylandEnable=false
```

### 3. 保存文件并重启显示管理器

```bash
sudo systemctl restart gdm3
```

### 4. 重新登录系统

- 系统会自动重启显示服务

- 在登录界面右下角选择 "Ubuntu on Xorg"

- 输入密码登录

***

## ✅ 验证解决方案

1. 启动 ToDesk

2. 尝试从其他设备远程连接

3. 检查是否可正常显示和控制

***

## 🔄 替代方案：登录时切换显示服务器

如果不想永久禁用 Wayland：

1. 在登录界面

2. 点击用户名右下角的齿轮图标

3. 选择 **"Ubuntu on Xorg"**

4. 输入密码登录

> **注意**：此方法需要每次登录时手动选择

***

## ⚠️ 其他注意事项

### 1. 检查 ToDesk 服务状态

```bash
systemctl status todeskd.service
```

### 2. 重新安装 ToDesk

如果问题依旧：

```bash
# 卸载
sudo apt remove todesk

# 重新下载安装包
wget https://dl.todesk.com/linux/todesk_4.3.1.0_amd64.deb

# 安装
sudo dpkg -i todesk_4.3.1.0_amd64.deb
sudo apt -f install
```

### 3. 防火墙设置

确保开放端口：

```bash
sudo ufw allow 21780:21789/tcp
sudo ufw allow 21780:21789/udp
```

### 4. 硬件加速问题

如果使用 NVIDIA 显卡：

```bash
sudo prime-select on-demand  # 切换显卡模式
sudo reboot
```

***

## 🌐 支持 Wayland 的替代远程工具

| 工具名称                      | 安装命令                                      | 特点              |
| ------------------------- | ----------------------------------------- | --------------- |
| **RustDesk**              | `sudo apt install rustdesk`               | 开源免费，支持 Wayland |
| **AnyDesk**               | `sudo apt install anydesk`                | 商业软件，兼容性好       |
| **Chrome Remote Desktop** | [官网下载](https://remotedesktop.google.com/) | 浏览器集成，跨平台       |

***

 

> **💡 提示**：
>
> - 此方案适用于 Ubuntu 20.04/22.04/24.04
>
> - 修改前建议备份配置文件：\
>   `sudo cp /etc/gdm3/custom.conf /etc/gdm3/custom.conf.bak`
>
> - 关注 [ToDesk 官网](https://www.todesk.com/) 获取最新 Wayland 支持进展
