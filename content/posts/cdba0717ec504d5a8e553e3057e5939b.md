---
title: Android 15 WiFi AP 与热点名称修改
slug: cdba0717ec504d5a8e553e3057e5939b
tags:
  - android
date: 2026-01-28T12:31:36.110Z
---

# Android 15 WiFi AP 与热点名称修改

📁 修改概述

 

本次修改涉及两个文件，均为 Wifi 服务模块下的 Java 代码：

1. **WifiApConfigStore.java**\
   修改默认 AP 配置中的 SSID 名称为设备型号名称。

2. **WifiP2pServiceImpl.java**\
   修改 P2P 热点默认设备名称为设备型号名称。

***

### 📄 详细 diff 内容

```diff
--- a/system/packages/modules/Wifi/service/java/com/android/server/wifi/WifiApConfigStore.java
+++ b/system/packages/modules/Wifi/service/java/com/android/server/wifi/WifiApConfigStore.java
@@ -426,8 +426,17 @@ public class WifiApConfigStore {
     private SoftApConfiguration getDefaultApConfiguration() {
         SoftApConfiguration.Builder configBuilder = new SoftApConfiguration.Builder();
         configBuilder.setBand(generateDefaultBand(mContext));
+        
+        //kte zhangkai 2026-1-15, config wifi ssid.
+        String ssidName = android.os.SystemProperties.get("ro.kte.kte_product_model", "");
+        if(ssidName != null){
+        configBuilder.setSsid(ssidName);
+        }else {
         configBuilder.setSsid(mContext.getResources().getString(
                 R.string.wifi_tether_configure_ssid_default) + "_" + getRandomIntForDefaultSsid());
+        }        
+        //kte zhangkai 2026-1-15, config wifi ssid.
+        
         try {
             if (ApConfigUtil.isWpa3SaeSupported(mContext)) {
                 configBuilder.setPassphrase(generatePassword(),
diff --git a/kte_project/android/public/system/packages/modules/Wifi/service/java/com/android/server/wifi/p2p/WifiP2pServiceImpl.java b/kte_project/android/public/system/packages/modules/Wifi/service/java/com/android/server/wifi/p2p/WifiP2pServiceImpl.java
old mode 100644
new mode 100755
index e004bc4660d..a8520c8d086
--- a/kte_project/android/public/system/packages/modules/Wifi/service/java/com/android/server/wifi/p2p/WifiP2pServiceImpl.java
+++ b/kte_project/android/public/system/packages/modules/Wifi/service/java/com/android/server/wifi/p2p/WifiP2pServiceImpl.java
@@ -6760,7 +6760,15 @@ public class WifiP2pServiceImpl extends IWifiP2pManager.Stub {
             mDefaultDeviceName = prefix + postfix;
             mLastDefaultDeviceNameGeneratingTimeMillis = mClock.getElapsedSinceBootMillis();
             logd("the default device name: " + mDefaultDeviceName);
-            return mDefaultDeviceName;
+            
+            //kte zhangkai 2026-1-15,config device name.
+            String kteDeviceName = android.os.SystemProperties.get("ro.kte.kte_product_model", "");
+            if(kteDeviceName != null){
+            	return kteDeviceName;
+            }else {
+            	return mDefaultDeviceName;
+            }
+            //kte zhangkai 2026-1-15,config device name.
         }
 
         private String generateP2pSsidPostfix(String devName) {

```

### 🔧 修改逻辑说明

| 文件                        | 修改位置                             | 逻辑说明                                                                  |
| ------------------------- | -------------------------------- | --------------------------------------------------------------------- |
| `WifiApConfigStore.java`  | `getDefaultApConfiguration()` 方法 | 优先使用 `ro.kte.kte_product_model` 系统属性作为 AP 的 SSID 名称，若为空则使用原默认名称拼接随机数。 |
| `WifiP2pServiceImpl.java` | `getDefaultDeviceName()` 方法      | 优先使用 `ro.kte.kte_product_model` 系统属性作为 P2P 热点设备名称，若为空则使用原生成的默认设备名称。   |

***

### 🧩 系统属性说明

- **属性名**: `ro.kte.kte_product_model`

- **用途**: 存储设备型号名称，例如 `KTE_A100`、`KTE_B200` 等。

- **设置方式**: 通常在设备 `build.prop` 或设备定制代码中设置。

***

### ✅ 测试建议

1. 编译系统并刷机。

2. 开启 WiFi 热点，检查 SSID 是否为设备型号名称。

3. 开启 WiFi 直连（P2P），检查设备名称是否为设备型号名称。

4. 若系统属性未设置，应回退到原默认命名规则（带随机后缀）。

***

### 📎 备注

- 该修改为公版通用修改，适用于所有使用该代码分支的设备。

- 需确保 `ro.kte.kte_product_model` 在设备出厂前已正确写入系统属性。

***
