---
title: Linux Python 版本切换指南
slug: 4bf4d159314b47d1afce7cdf3b2c397c
tags:
  - python
  - linux
date: 2025-06-10T03:30:12.088Z
---

## 🔧 核心命令

```bash
sudo update-alternatives --config python
```

## 📍 使用场景

当系统安装多个 Python 版本时，通过此命令：

1. 查看所有可用 Python 版本

2. 交互式选择默认 Python 解释器

3. 切换全局 Python 命令指向的版本

***

## 🛠️ 完整配置流程

### 1. 安装 Python 版本

```bash
# 安装 Python 3.8
sudo apt install python3.8

# 安装 Python 3.10
sudo apt install python3.10
```

### 2. 注册 Python 版本到 alternatives 系统

```bash
# 注册 Python 3.8
sudo update-alternatives --install /usr/bin/python python /usr/bin/python3.8 1

# 注册 Python 3.10
sudo update-alternatives --install /usr/bin/python python /usr/bin/python3.10 2
```

**参数说明**：

- `/usr/bin/python`：符号链接位置

- `python`：备选组名称

- `/usr/bin/python3.x`：实际解释器路径

- 末尾数字：优先级（越大优先级越高）

### 3. 查看已注册版本

```bash
update-alternatives --list python
```

或

```bash
update-alternatives --display python
```

### 4. 切换版本

```bash
sudo update-alternatives --config python
```

输出示例：

```text
There are 2 choices for the alternative python (providing /usr/bin/python).

  Selection    Path                Priority   Status
------------------------------------------------------------
* 0            /usr/bin/python3.10   2        auto mode
  1            /usr/bin/python3.8    1        manual mode
  2            /usr/bin/python3.10   2        manual mode

Press <enter> to keep the current choice[*], or type selection number:
```

输入对应编号后回车即可切换

***

## ⚙️ 其他实用命令

### 设置自动模式（最高优先级）

```bash
sudo update-alternatives --auto python
```

### 删除已注册版本

```bash
sudo update-alternatives --remove python /usr/bin/python3.8
```

### 临时使用特定版本

```bash
# 不改变全局设置
python3.8 script.py
```

***

## ⚠️ 重要注意事项

1. **系统依赖问题**\
   许多系统工具依赖 Python 3.x（如 `apt`），避免将默认 Python 设置为 2.x

2. **虚拟环境优先**

   ```bash
   # 推荐使用 venv
   python3.8 -m venv myenv
   source myenv/bin/activate
   ```

3. **路径验证**\
   切换后检查实际路径：

   ```bash
   which python
   python --version
   ```

***

## 🔄 替代方案：`pyenv`（多版本管理）

```bash
# 1. 安装 pyenv
curl https://pyenv.run | bash

# 2. 安装 Python 版本
pyenv install 3.8.12
pyenv install 3.10.4

# 3. 全局切换
pyenv global 3.10.4

# 4. 局部切换（当前目录）
pyenv local 3.8.12
```

### 对比 `update-alternatives` vs `pyenv`

| 特性          | update-alternatives | pyenv  |
| ----------- | ------------------- | ------ |
| 系统级修改       | ✅                   | ❌（用户级） |
| 多版本并行       | ❌                   | ✅      |
| 自动安装 Python | ❌                   | ✅      |
| 需要 sudo 权限  | ✅                   | ❌      |
| 切换速度        | 快                   | 快      |
| 隔离性         | 弱                   | 强      |

***

## 🐞 常见问题解决

### 问题：`update-alternatives: error: no alternatives for python`

**原因**：未注册任何 Python 版本\
**解决**：

```bash
sudo update-alternatives --install /usr/bin/python python /usr/bin/python3 10
```

### 问题：切换后系统工具报错

**场景**：如 `apt` 报错 "ModuleNotFoundError"\
**解决**：恢复系统默认 Python

```bash
sudo update-alternatives --config python  # 选择系统默认版本
# 或
sudo ln -sf /usr/bin/python3.10 /usr/bin/python
```

### 问题：无符号链接权限

**解决**：使用用户级别名

```bash
# 添加到 ~/.bashrc
alias python='/usr/bin/python3.10'
alias pip='/usr/bin/pip3.10'
```

***

> **📚 推荐实践**
>
> 1. 保留系统 Python 不动（通常为 python3.10）
>
> 2. 开发环境使用 `pyenv` 或 `venv`
>
> 3. 需要全局切换时用 `update-alternatives`
