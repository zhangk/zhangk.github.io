baseURL = "/"
languageCode = "zh-cn"
timeZone = "Asia/Shanghai"
title = "ZhangK’s Blog"
author = "ZhangK"
theme = "LoveIt"

# ---------- 关键修复 ----------
# 使用绝对路径，而不是相对路径
relativeURLs = false
# 确保URL规范化
canonifyURLs = true
# ---------- ---------- ----------

[outputs]
  home = ["HTML", "JSON", "RSS"]
[params.search]
  enable = true
  type = "algolia"  # 类型改为 algolia
  contentLength = 4000
  maxResultLength = 10
  [params.search.algolia]
    index = "zhangk_blog"
    appID = "30WY2DYYB1"
    searchKey = "cdf22c87b449280f726df09efc58b84a"
